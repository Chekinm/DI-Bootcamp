const nums = require('./main.js');
const http = require('http')

const b = 5;
result = nums.largeNumber + b

console.log(nums.largeNumber + b)
datestring = nums.datestring


module.exports = {
    result, datestring
}